{% extends 'skeleton.html.twig' %}


{% block body %}
<body>
{{ parent() }}
<div class="example-wrapper">
    <div class="card mb-3">
        <img class="card-img-top" src="{{ asset(article.image) }}" alt="Card image cap">
        <div class="card-body">
            <h5 class="card-title">{{ article.title }} - {{ article.id }}</h5>
            <p class="card-text">{{ article.description }}</p>
            <p class="card-text"><small class="text-muted">Ajouté le: {{ article.date.date }}</small></p>
        </div>
    </div>

</div>
</body>
{% endblock %}
